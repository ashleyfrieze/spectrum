package com.greghaskins.spectrum;

/**
 * Tagging interface, illustrates an atomic test context. Rules are applied over atomic
 * tests, which are usually {@link Spec} but may be composites - i.e. {@link Suite}
 * masquerading as a single test.
 */
public interface Atomic {
}
